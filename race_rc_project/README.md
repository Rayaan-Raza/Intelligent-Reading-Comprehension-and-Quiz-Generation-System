# race_rc_project

Starter structure for preprocessing, feature engineering, model training, inference, and UI.

## Quick start

1. Create a virtual environment.
2. Install dependencies from `requirements.txt`.
3. Place dataset CSV files in `data/raw/`.
4. Run training scripts in `src/`.
